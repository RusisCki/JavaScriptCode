const originalPrice = 15000;
let member = "Silver" ;
undefined
if (member ==="Brown" )
{
    totalPrice += originalPrice * 0.9 ;

} else if ( member === "Silver" )
{
    totalPrice += originalPrice * 0.8 ;
} else if (member === "Paltin" )
{
    totalPrice += originalPrice * 0.77 ;
}
12000
if (member ==="Brown" )
{
    totalPrice += originalPrice * 0.9 ;

} else if ( member === "Silver" )
{
    totalPrice += originalPrice * 0.8 ;
} else if (member === "Paltin" )
{
    totalPrice += originalPrice * 0.77 ;
}else 
{
    totalPrice += originalPrice ;
}
24000
totalPrice = 0 ;
0
member = "Platin" ;
'Platin'
if (member ==="Brown" )
{
    totalPrice += originalPrice * 0.9 ;

} else if ( member === "Silver" )
{
    totalPrice += originalPrice * 0.8 ;
} else if (member === "Paltin" )
{
    totalPrice += originalPrice * 0.77 ;
}else 
{
    totalPrice += originalPrice ;
}
15000
// Platin != Paltin
undefined
member = "Paltin" ;
'Paltin'
if (member ==="Brown" )
{
    totalPrice += originalPrice * 0.9 ;

} else if ( member === "Silver" )
{
    totalPrice += originalPrice * 0.8 ;
} else if (member === "Paltin" )
{
    totalPrice += originalPrice * 0.77 ;
}else 
{
    totalPrice += originalPrice ;
}

26550
let totalPrice = 0 ;
undefined
'Platin'
if (member ==="Brown" )
{
    totalPrice += originalPrice * 0.9 ;

} else if ( member === "Silver" )
{
    totalPrice += originalPrice * 0.8 ;
} else if (member === "Paltin" )
{
    totalPrice += originalPrice * 0.77 ;
}else 
{
    totalPrice += originalPrice ;
}

11550